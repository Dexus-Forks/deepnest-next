# Story 3.1 — Traceability Matrix (TEA / `bmad-tea-trace`)

> Generated by **Murat (TEA)** on 2026-04-26 via `bmad-tea-trace` for **DEE-70 / post-merge** of [PR #7](https://github.com/Dexus-Forks/deepnest-next/pull/7) (squash-merged at `73daa8a4`). This is the canonical AC ↔ code ↔ test traceability artefact for Story 3.1 per its DoD.

- **Story file:** `_bmad-output/implementation-artifacts/3-1-implement-tests-assets-integrity-check-manifest-wire-into-npm-test.md`
- **Merged commit:** `73daa8a4d959528bb651f2a5232f4da5490978f8` on `main`
- **PR head SHA (pre-squash):** `39a7e962d3fe6984e9776455397b20b59a1cb5e2`
- **Sprint:** MVP-1 / Stream B head (DEE-44 / DEE-53 / DEE-54 / DEE-56 → DEE-63 → DEE-70)
- **Gate decision:** **PASS** — see `3-1-gate-decision.md`

---

## 1. Risk-priority assignment (P0–P3)

Per Murat risk matrix: `Probability × Impact → P0–P3`. Probability is "how likely a change in scope drifts the AC?", Impact is "what breaks if the AC fails?".

| AC | Title | Probability | Impact | **P-rank** | Why |
|---|---|---|---|---|---|
| AC-03.1 | Integrity check runs first in `npm test`, fail-fast | High | High | **P0** | The whole gate is moot if the chain doesn't fail-fast — Playwright would still run on a drifted fixture set and either flake or assert on stale literals. |
| AC-03.2 | No new GitHub Actions job | Mid | High | **P1** | Architecture overlay constraint (FR-03 (a)). Adding a workflow would split CI ownership across two surfaces — high blast radius for future maintenance. |
| AC-03.3 | Failure mode names both literals + docs pointer | Mid | High | **P1** | Architecture mandate (FR-03 (b)). Without this, a contributor seeing red CI doesn't know what to fix → MTTR explodes. |
| AC-03.4 | Manifest captures fixture set + literals | High | High | **P0** | The committed manifest IS the integrity contract. If it's missing or wrong-shaped, the check is decorative. |
| AC-03.5 | Day-one false-positive free + red transcript demo | High | High | **P0** | The whole sprint blocks if main fails on the new check — this is the install-time smoke. |
| AC-03.6 | Test-latency rule respected (< 1 s on dev laptop) | Mid | Mid | **P2** | Architecture overlay §3.3. Soft constraint — a slow check still works; it just slows commits. AC-03.10 covers the CI dimension. |
| AC-03.7 | Read-only over the fixture set | Low | High | **P1** | Anti-pattern §16.10 — re-encoding a fixture without re-deriving spec literals is the failure mode this story closes. The story itself MUST NOT introduce that pattern. |
| AC-03.8 | Spec literals unchanged | Low | High | **P1** | Same anti-pattern surface as AC-03.7. The check reads the spec; it does not edit it. |
| AC-03.9 | `project-context.md` §16 anti-patterns hold (16-item audit) | Mid | High | **P1** | Project-wide invariants — any one of the 16 violated would be a regression beyond Story 3.1. |
| AC-03.10 | `npm test` regression-free vs. NFR-01 baseline (±20 %) | Low | High | **P1** | Sprint-Start gate G-1 anchor (DEE-52). The chain insertion budget is sub-200 ms; tolerance is ±3 349 ms. Math says infeasible — but CI evidence is the only canonical proof. |

**Risk distribution:** **P0 ×3** (AC-03.1, AC-03.4, AC-03.5), **P1 ×6** (AC-03.2, AC-03.3, AC-03.7, AC-03.8, AC-03.9, AC-03.10), **P2 ×1** (AC-03.6), **P3 ×0**.

**Trace coverage rule (Murat principles):** **All P0 + P1 must be fully traced for PASS.** P2 must be traced or have a documented bypass; P3 may be smoke-only.

---

## 2. Acceptance criterion ↔ code ↔ test/evidence

Each row maps the AC to the implementation locus on `main` post-merge (`73daa8a4`) and the canonical evidence that proves it. Tests/evidence types: **E2E** = Playwright run via `npm test`; **STATIC** = literal grep / `git diff` invariant; **SCRIPT** = behavioural verification of the integrity check itself (red + green); **CI** = GitHub Actions workflow run output.

| AC | P-rank | Code (file : symbol / line) | Evidence (kind + locator) | Status |
|---|---|---|---|---|
| **AC-03.1** | **P0** | `package.json` line 18 `"test": "npm run test:fixtures:check && playwright test"` | **CI** — run `24956426721` step "Run Playwright tests" log: `> deepnest@1.5.6 test\n> npm run test:fixtures:check && playwright test\n> deepnest@1.5.6 test:fixtures:check\n> node scripts/check-test-fixtures.mjs` then `Running 1 test using 1 worker` after. Chain executes left-to-right, fail-fast via `&&`. | **PASS** ✅ |
| **AC-03.2** | **P1** | `.github/workflows/playwright.yml` (untouched), no new workflow file | **STATIC** — `git diff main..73daa8a4 --stat -- .github/workflows/` returns exit 0 / no rows; `gh pr view 7 --json files` shows no `.github/workflows/*` entry. | **PASS** ✅ |
| **AC-03.3** | **P1** | `scripts/check-test-fixtures.mjs:166–174` (`failSpecFormat` — exit 2) and `:196–209` (drift block — exit 1, names `importsnav_count` + `placements_total/max` and points at `docs/development-guide.md §"Re-deriving the test-fixture literals after a tests/assets/ change"`) | **SCRIPT** — story Dev Agent Record `Debug Log References` line: "drift diff names both literals + names the drifted file ✓". Self-Review Phase-4 verification matrix row AC-03.3: "verified across 3 adversarial scenarios: byte append, rename, new-subdir-file". | **PASS** ✅ |
| **AC-03.4** | **P0** | `tests/assets/.fixture-manifest.json` (committed, 21 lines) — keys: `schema_version=1`, `captured_at`, `literals.{importsnav_count, placements_total, placements_max}`, `files[].{path, size, sha256}`. Builder: `scripts/check-test-fixtures.mjs:94–105 buildManifest()`. | **STATIC** — `git ls-tree --name-only 73daa8a4 tests/assets/.fixture-manifest.json` returns the file; payload matches the recommended template (story §AC-03.4 example) byte-for-byte (modulo `captured_at` which is the only varying field). | **PASS** ✅ |
| **AC-03.5** | **P0** | Behavioural — green path: `scripts/check-test-fixtures.mjs:176–210 modeCheck()` exits 0 on the committed manifest. Red path: same function exits 1 on byte-append / rename / new-file scenarios. | **CI** — run `24956426721` (post-merge, `xvfb-run npm test`) green: `1 passed (16.7s)`. **SCRIPT** (red transcripts) — story Debug Log References lines: `echo " " >> tests/assets/henny-penny.svg && node scripts/check-test-fixtures.mjs → exit 1, drift diff names both literals + names the drifted file ✓` and Self-Review §6 (rename + new-subdir-file scenarios). | **PASS** ✅ |
| **AC-03.6** | **P2** | `scripts/check-test-fixtures.mjs:33–55 listFixtures()` — synchronous `readdirSync({recursive: true})` + `readFileSync` + `crypto.createHash('sha256')` only, no fork, no spawn. | **SCRIPT** — story Debug Log References p95 ≈ 76 ms across 5 dev-laptop runs (`56 / 59 / 61 / 68 / 76 ms`). 13× margin under the 1 s budget. **CI** — run `24956426721` log: `test:fixtures:check` lines span `12:19:20.7919` → `12:19:20.8978` ≈ **106 ms** end-to-end (npm-run overhead included). | **PASS** ✅ |
| **AC-03.7** | **P1** | Files under `tests/assets/`: `henny-penny.svg`, `mrs-saint-delafield.svg`, `.fixture-manifest.json` (new). | **STATIC** — `git diff main..73daa8a4 -- tests/assets/*.svg` returns empty; only addition under `tests/assets/` is `.fixture-manifest.json`. Verified at branch HEAD by Self-Review verification matrix row AC-03.7. | **PASS** ✅ |
| **AC-03.8** | **P1** | `tests/index.spec.ts` (untouched) | **STATIC** — `git diff main..73daa8a4 -- tests/index.spec.ts` returns empty. Self-Review row AC-03.8 confirms. | **PASS** ✅ |
| **AC-03.9** | **P1** | `package.json` (only `scripts.*` entries + `build.files` exclusion changed; no new `dependencies`); pre-commit hook unchanged; `eslint.config.mjs` unchanged. | **STATIC** — story §16 audit map (16 rows) + Self-Review architecture conformity block. PR description embeds the 16-item checklist inline. `git log 73daa8a4 -1 --pretty=fuller` shows no `--no-verify` flag. | **PASS** ✅ |
| **AC-03.10** | **P1** | Chain insertion is the only NFR-01 surface change (`scripts.test` literal). | **CI** — run `24956426721` step "Run Playwright tests" wall-clock = **18 323 ms** (start `12:19:20.187` / end `12:19:38.510`). Baseline `rolling_mean_ms = 16 746.6`, ±20 % → window [13 397.3, 20 095.9] ms. Delta **+1 576.4 ms / +9.41 %** → **WITHIN tolerance**. See `3-1-nfr01-ci-evidence.md` for full breakdown including pre-merge run cross-check. | **PASS** ✅ |

**Trace completeness:**
- P0 traced: **3/3** ✅
- P1 traced: **6/6** ✅
- P2 traced: **1/1** ✅ (over-and-above the floor)
- P3 traced: **0/0** (none in scope)

**No untraced ACs. No open trace gaps.**

---

## 3. NFR coupling

| NFR | Capability ↔ NFR pair | Traced via | Status |
|---|---|---|---|
| **NFR-01** (latency, ±20 % vs. baseline) | `npm test` chain wall-clock | AC-03.10 (CI run `24956426721`) → see `3-1-nfr01-ci-evidence.md` | **SATISFIED** ✅ (delta +9.41 %, well within ±20 % budget) |
| **NFR-05** (capability ↔ NFR pair: fixture-integrity gate exists and is enforced) | `npm test` chain enforces the check before Playwright | AC-03.1 (chain shape literal) + AC-03.4 (manifest committed) + AC-03.5 (red+green transcripts) | **SATISFIED** ✅ |

NFR-01 + NFR-05 are the two NFRs Story 3.1 explicitly couples to (per story "NFR coupling measured" line). Both are traced through P0/P1 ACs above.

---

## 4. Anti-pattern audit (project-context.md §16)

Direct re-validation against the merged diff. **None violated.**

| §16.X | Anti-pattern | Verdict | Evidence |
|---|---|---|---|
| 1 | New global on `window` | N/A | `git diff main..73daa8a4 -- main/` empty |
| 2 | `ipcRenderer.invoke('read-config'\|'write-config')` outside `config.service.ts` | N/A | No IPC surface touched |
| 3 | New `background-*` IPC handler outside `nesting.service.ts` | N/A | No IPC surface touched |
| 4 | New UI framework | N/A | No UI surface touched |
| 5 | Modify vendored utilities in `main/util/` | N/A | `main/util/` not in PR diff |
| 6 | Import from `main/util/_unused/` | N/A | No imports added |
| 7 | New TypeScript decorator transform | N/A | Script is `.mjs`, no TS |
| 8 | New `// @ts-ignore` | N/A | Script is `.mjs`, no TS |
| 9 | `--no-verify` on commit | **Not violated** | `git log main..73daa8a4 --pretty=fuller` — no skip-hook commits |
| 10 | Drop / re-encode `tests/assets/*.svg` without re-deriving spec literals | **Story closes the gate against this — does not violate** | This is the anti-pattern the story exists to enforce against |
| 11 | Double-convert mm/inch | N/A | No unit math |
| 12 | Open external URLs by spawning a `BrowserWindow` | N/A | No `BrowserWindow` |
| 13 | Add HTTP server / telemetry / DB | N/A | Zero-dep Node script |
| 14 | Assume Windows `clean`/`clean-all` works on Linux/macOS | N/A | Story does not run those scripts |
| 15 | Remove `**/*.js` from ESLint global ignore | **Not violated** | `git diff main..73daa8a4 -- eslint.config.mjs` empty |
| 16 | New spinner glyph | N/A | `main/img/` not in PR diff |

---

## 5. Coverage summary + gate alignment

```
P0 + P1 ACs:        9 / 9 traced
Total ACs:         10 / 10 traced
NFR couplings:      2 / 2 satisfied (NFR-01, NFR-05)
Anti-pattern §16:  16 / 16 audited (0 violations)
Test types:        E2E (CI) + STATIC + SCRIPT — all three exercised
CI evidence:       run 24956426721 (post-merge) — green, within NFR-01 tolerance
```

**Gate input feeders → gate decision (`3-1-gate-decision.md`): PASS.**

---

## References

- Story file: `_bmad-output/implementation-artifacts/3-1-implement-tests-assets-integrity-check-manifest-wire-into-npm-test.md`
- Gate decision: `_bmad-output/quality-artifacts/3-1-gate-decision.md`
- NFR-01 CI evidence detail: `_bmad-output/quality-artifacts/3-1-nfr01-ci-evidence.md`
- NFR-01 baseline: `_bmad-output/planning-artifacts/nfr01-baseline.json`
- Architecture: `_bmad-output/planning-artifacts/architecture.md` §3.1 / §3.3 / §4 (FR-03)
- PRD: `_bmad-output/planning-artifacts/prd.md` §FR-03
- Project context: `_bmad-output/project-context.md` §12 / §16
- PR: https://github.com/Dexus-Forks/deepnest-next/pull/7
- CI run (canonical): https://github.com/Dexus-Forks/deepnest-next/actions/runs/24956426721
- Parent issue: DEE-70 — Story 3.1 — bmad-tea-trace traceability matrix + NFR-01 CI wall-clock confirmation (post-merge)
